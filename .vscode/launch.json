{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Fn-Toggle Script",
            "type": "node-terminal",
            "request": "launch",
            "command": "${workspaceFolder}/fn-toggle.sh",
            "cwd": "${workspaceFolder}"
        }
    ]
}

